{
  "$comment": [
    "Every number this page draws, tied to the committed blob it came from.",
    "",
    "The page's thesis is 'every number is traceable to the repository it came",
    "from'. Until this file existed that was a slogan: the plates and the prose",
    "were checked against EACH OTHER (gate.mjs check 9) and against nothing else,",
    "so two authored strings could agree perfectly and both be false. Three rounds",
    "of audits found exactly that.",
    "",
    "A row is a derivation, not a citation. `extractor` is a shell command run",
    "against $BLOB — the file fetched from raw.githubusercontent.com at `ref`,",
    "which is a COMMIT SHA, never a branch name. Its stdout must equal `value`",
    "exactly. Nobody has to trust the number; they run the command.",
    "",
    "`drawn_on` is where the value appears on this page. build/claims.mjs checks",
    "both directions: every row must re-derive, AND every number a plate draws",
    "must have a row here or an entry in `exempt`. A claims file that only",
    "verified the numbers it happened to list would be the same gate-that-cannot-",
    "fail this project has already shipped twice."
  ],
  "$repin_note": [
    "Re-pinned 2026-08-06 to the branch heads a full six-repo audit re-derived",
    "every claim against — 51 of 52 values held at both the old pin and the new",
    "one, and the 52nd (glyph.msimd128_line) is a line number that moved with",
    "its file and is corrected below. Every old pin remains an ancestor of its",
    "new one: no force-pushes, no rewritten history. The applied-web entry is",
    "gone because the split it named stopped existing — see the note on the",
    "applied repo and the ONNX rows."
  ],
  "$repin_note_2026_08_10": [
    "Cadence re-pinned 8a2fbbb -> 771acc48, 32 commits, ancestry verified. This one",
    "was not housekeeping. The old pin was holding section V's claim that deployed",
    "RLS was 'staged rather than load-bearing' because production still connected as",
    "an owner role with BYPASSRLS. True at 8a2fbbb; false since 2026-08-03, when the",
    "DATABASE_URL swap landed and production moved to cadence_app (NOSUPERUSER",
    "NOBYPASSRLS). Confirmed four ways before the pin moved: the 0003 migration, the",
    "live pg_roles row, docs/RLS-CUTOVER.md, and the deployed environment itself.",
    "",
    "The lesson is about the gate, not the pin. claims.mjs re-derives NUMBERS and had",
    "been green throughout -- 36 handlers was correct at 8a2fbbb. The false sentence",
    "was PROSE, which nothing here reads. The freshness check had been printing",
    "'cadence is pinned N commits behind main' for a week and does not fail the build,",
    "by design. So the page understated its own strongest security result, in public,",
    "with every gate passing. That boundary is now stated on the page itself rather",
    "than left for a reader to discover.",
    "",
    "Both glyph benchmark rows re-sourced the same day -- see the notes on",
    "glyph.dot256_speedup and glyph.axpy128_slowdown. Short version: they cited a",
    "December CSV recorded on a different machine, which glyph's own ENVIRONMENT.md",
    "had already retired. axpy moves 6.9 -> 10.7; dot stays 3.5 because the run that",
    "glyph names as canonical happens to agree with the CSV to one decimal. Had the",
    "aggregated pair been chosen instead, dot would have read 3.6, and a headline",
    "number would have moved as a side effect of a sourcing fix nobody was watching."
  ],
  "repos": {
    "glyph": {
      "github": "yadava5/glyph",
      "ref": "3e510d497c3ad43b390c1845864f546f0e8b1543",
      "branch": "main",
      "public": true,
      "note": "Re-pinned 2026-08-08 because the DEPLOYMENT moved and the page's strongest claim is about the deployment. de7f24ac is an ancestor, 13 commits back, no rewritten history. The browser build was rebuilt somewhere in those 13, so the served binary changed size and hash; it is still byte-identical to this ref's blob, which is the thing the sentence actually argues."
    },
    "jetpack": {
      "github": "yadava5/jetpack-compress",
      "ref": "7b75e03ddd6bb1eb6708eff6b10aacb8148a7c74",
      "branch": "main",
      "public": true
    },
    "cadence": {
      "github": "yadava5/cadence",
      "ref": "771acc48cb4e3665b474cc73652a8e2c2cfc4322",
      "branch": "main",
      "public": true,
      "note": "Re-pinned 2026-08-10, 32 commits on from 8a2fbbb (verified an ancestor: git merge-base --is-ancestor passes, no rewritten history). The old pin was not merely behind — it was holding a sentence that had become FALSE. At 8a2fbbb, docs/RLS-CUTOVER.md still read 'the app connects as postgres, which carries BYPASSRLS', and section V repeated it. The DATABASE_URL swap was made 2026-08-03 (b271100); production connects as cadence_app, NOSUPERUSER NOBYPASSRLS, confirmed against the live database and the deployed environment. The freshness check at claims.mjs:388 had been printing 'pinned N commits behind main' the whole time and does not fail the build, by design — which is exactly how a prose claim outlived its evidence. handlers moves 36 -> 37 with this pin."
    },
    "applied": {
      "github": "yadava5/applied",
      "ref": "5b895d8dd30ae6ae502d7ae29b830429a85c5c3f",
      "branch": "main",
      "public": true,
      "note": "This pin now carries ml/ — the web-migration branch merged into main on 2026-08-03 via merge commit d349f02f, without a PR, which is why no gate saw it coming. The separate applied-web entry that used to pin that branch is retired: both ONNX byte counts cite main now, at identical sizes."
    },
    "automl": {
      "github": "yadava5/ai-augmented-auto-ml-toolchain",
      "ref": "5c5b7624d4ede05235fc0289975d59f401e6a4e4",
      "branch": "main",
      "public": true,
      "note": "Public since 2026-07-30. Every AutoML figure on this page was ATTESTED before that and is now DERIVED, which is the whole point of the distinction. Written with Shree Chaturvedi; the repository has disjoint histories, so main is the only branch worth pinning."
    },
    "visualassist": {
      "github": "yadava5/VisualAssist",
      "ref": "b0e6203369a829e2174ea263be53be0287675af9",
      "branch": "main",
      "public": true,
      "note": "New in round 21, at the client's choice. Swift/ARKit/LiDAR; no deployment exists, and the page says why instead of faking a link. The MUCAT placement and the $2,500 grant it is the artifact for stay ATTESTED — placements are not derivable from a repository."
    }
  },
  "claims": [
    {
      "id": "glyph.accuracy_pct",
      "value": "97.01",
      "unit": "%",
      "drawn_on": [
        "plate-1-glyph.svg",
        "m-1-glyph.svg",
        "README.md"
      ],
      "repo": "glyph",
      "path": "benchmarks/mnist_eval.json",
      "extractor": "jq -r '(.overall.accuracy_pct*100|round)/100' \"$BLOB\""
    },
    {
      "id": "glyph.test_set_size",
      "value": "10000",
      "drawn_on": [
        "plate-1-glyph.svg",
        "README.md"
      ],
      "repo": "glyph",
      "path": "benchmarks/mnist_eval.json",
      "extractor": "jq -r '.dataset.images' \"$BLOB\""
    },
    {
      "id": "glyph.wrong",
      "value": "299",
      "drawn_on": [
        "plate-1-glyph.svg",
        "README.md"
      ],
      "repo": "glyph",
      "path": "benchmarks/mnist_misclassified.csv",
      "extractor": "tail -n +2 \"$BLOB\" | grep -c .",
      "note": "Counted from the error list itself, not from 10000-9701 — so the 299 marks the plate draws and the number beside them come from one artifact."
    },
    {
      "id": "glyph.confident_errors",
      "value": "79",
      "drawn_on": [
        "plate-1-glyph.svg",
        "README.md"
      ],
      "repo": "glyph",
      "path": "benchmarks/mnist_misclassified.csv",
      "extractor": "tail -n +2 \"$BLOB\" | awk -F, '$4>0.9' | grep -c .",
      "note": "Column 4 is pred_activation — the softmax mass on the label it wrongly chose."
    },
    {
      "id": "glyph.deployed_wasm_bytes",
      "value": "43751",
      "unit": "bytes",
      "drawn_on": [
        "README.md"
      ],
      "live": "https://getglyph.vercel.app/wasm/fast_mnist.wasm",
      "header": "content-length",
      "extractor": "echo \"$HEADER\"",
      "note": "A claim about the DEPLOYED artifact, not a committed one — checked against the deployment with curl, exactly as the sentence describes. Deliberately uncached, and it broke the build the day the SIMD work landed on main and Vercel began serving a different binary. That is the point of it."
    },
    {
      "id": "glyph.deployed_wasm_type",
      "value": "application/wasm",
      "drawn_on": [
        "README.md"
      ],
      "live": "https://getglyph.vercel.app/wasm/fast_mnist.wasm",
      "header": "content-type",
      "extractor": "echo \"$HEADER\""
    },
    {
      "id": "glyph.served_wasm_sha256",
      "value": "e681d2f76d41305aa3b8c250799f898bd1139497f60580ed59000d49cf5d6360",
      "drawn_on": [
        "README.md"
      ],
      "live": "https://getglyph.vercel.app/wasm/fast_mnist.wasm",
      "digest": "sha256",
      "extractor": "echo \"$HEADER\"",
      "note": "ADDED 2026-08-10, to close a hole the sibling row's own note had been documenting rather than fixing. glyph.deployed_wasm_sha256 hashes the REPO blob; glyph.deployed_wasm_bytes reads the DEPLOYMENT's content-length. Two sources, so a same-length different-bytes deploy passed both green — which is not hypothetical, it half-happened on 2026-08-08. This row runs the reader's own command against the host: curl the body, sha256 it, compare. With this and glyph.deployed_wasm_sha256 both green, the deployed bytes and the committed bytes are proven equal rather than separately plausible, and §III's 'this page's CI runs that same hash on every push' is a statement about machinery that exists."
    },
    {
      "id": "glyph.msimd128_line",
      "value": "332",
      "drawn_on": [
        "README.md"
      ],
      "repo": "glyph",
      "path": "CMakeLists.txt",
      "extractor": "grep -n -- '-msimd128' \"$BLOB\" | head -1 | cut -d: -f1",
      "note": "The README cites CMakeLists.txt:332 by line. Line citations drift silently as a file grows; this one now fails the build instead — and it earned its keep: it was 279 at the old pin, a coverage block and an eval target were inserted above it, and this row is what flagged the drift. The sentence you are reading said 330 until 2026-08-10, while the value beside it said 332: a note about line-citation drift, itself carrying a stale line citation."
    },
    {
      "id": "jetpack.parallel_mbs",
      "value": "422",
      "unit": "MB/s",
      "drawn_on": [
        "plate-2-jetpack.svg",
        "README.md"
      ],
      "repo": "jetpack",
      "path": "benchmarks/jmh-results-rigorous.json",
      "extractor": "jq -r '[.[]|select(.benchmark|endswith(\"parallelVirtualThreads\")).primaryMetric.score][0]/1e6|round' \"$BLOB\""
    },
    {
      "id": "jetpack.single_thread_mbs",
      "value": "66.2",
      "unit": "MB/s",
      "drawn_on": [
        "plate-2-jetpack.svg",
        "README.md"
      ],
      "repo": "jetpack",
      "path": "benchmarks/jmh-results-rigorous.json",
      "extractor": "jq -r '([.[]|select(.benchmark|endswith(\"singleThreadedJdk\")).primaryMetric.score][0]/1e6*10|round)/10' \"$BLOB\""
    },
    {
      "id": "jetpack.speedup",
      "value": "6.4",
      "unit": "x",
      "drawn_on": [
        "plate-2-jetpack.svg",
        "m-2-jetpack.svg",
        "README.md"
      ],
      "repo": "jetpack",
      "path": "benchmarks/jmh-results-rigorous.json",
      "extractor": "jq -r '(([.[]|select(.benchmark|endswith(\"parallelVirtualThreads\")).primaryMetric.score][0]/[.[]|select(.benchmark|endswith(\"singleThreadedJdk\")).primaryMetric.score][0])*10|round)/10' \"$BLOB\"",
      "note": "Derived from the two scores, not stored — so it cannot drift from them."
    },
    {
      "id": "jetpack.adler_scalar_gbs",
      "value": "1.52",
      "unit": "GB/s",
      "drawn_on": [
        "plate-2-jetpack.svg",
        "README.md"
      ],
      "repo": "jetpack",
      "path": "benchmarks/jmh-results-rigorous.json",
      "extractor": "jq -r '([.[]|select(.benchmark|endswith(\"Adler32Benchmark.scalar\")).primaryMetric.score][0]/1e9*100|round)/100' \"$BLOB\""
    },
    {
      "id": "jetpack.adler_vector_gbs",
      "value": "4.26",
      "unit": "GB/s",
      "drawn_on": [
        "plate-2-jetpack.svg",
        "README.md"
      ],
      "repo": "jetpack",
      "path": "benchmarks/jmh-results-rigorous.json",
      "extractor": "jq -r '([.[]|select(.benchmark|endswith(\"Adler32Benchmark.vector\")).primaryMetric.score][0]/1e9*100|round)/100' \"$BLOB\""
    },
    {
      "id": "jetpack.adler_jdk_intrinsic_gbs",
      "value": "14.06",
      "unit": "GB/s",
      "drawn_on": [
        "plate-2-jetpack.svg",
        "README.md"
      ],
      "repo": "jetpack",
      "path": "benchmarks/jmh-results-rigorous.json",
      "extractor": "jq -r '([.[]|select(.benchmark|endswith(\"jdkIntrinsic\")).primaryMetric.score][0]/1e9*100|round)/100' \"$BLOB\"",
      "note": "The row the page keeps because it loses to it."
    },
    {
      "id": "jetpack.adler_vector_speedup",
      "value": "2.80",
      "unit": "x",
      "drawn_on": [
        "plate-2-jetpack.svg",
        "README.md"
      ],
      "repo": "jetpack",
      "path": "benchmarks/jmh-results-rigorous.json",
      "extractor": "jq -r '([.[]|select(.benchmark|endswith(\"Adler32Benchmark.vector\")).primaryMetric.score][0]/[.[]|select(.benchmark|endswith(\"Adler32Benchmark.scalar\")).primaryMetric.score][0]*100|round)/100' \"$BLOB\" | awk '{printf \"%.2f\", $1}'"
    },
    {
      "id": "jetpack.ci_low",
      "value": "0.7",
      "unit": "%",
      "drawn_on": [
        "README.md"
      ],
      "repo": "jetpack",
      "path": "benchmarks/jmh-results-rigorous.json",
      "extractor": "jq -r '[.[]|.primaryMetric.scoreError/.primaryMetric.score*100]|min*10|round/10' \"$BLOB\""
    },
    {
      "id": "jetpack.ci_high",
      "value": "6.9",
      "unit": "%",
      "drawn_on": [
        "README.md"
      ],
      "repo": "jetpack",
      "path": "benchmarks/jmh-results-rigorous.json",
      "extractor": "jq -r '[.[]|.primaryMetric.scoreError/.primaryMetric.score*100]|max*10|round/10' \"$BLOB\""
    },
    {
      "id": "jetpack.forks",
      "value": "3",
      "drawn_on": [
        "plate-2-jetpack.svg",
        "README.md"
      ],
      "repo": "jetpack",
      "path": "benchmarks/jmh-results-rigorous.json",
      "extractor": "jq -r '.[0].forks' \"$BLOB\"",
      "anchors": {
        "README.md": [
          "a 3-fork JMH run",
          "2.80× on the 3-fork run"
        ]
      }
    },
    {
      "id": "jetpack.jdk",
      "value": "25",
      "drawn_on": [
        "plate-2-jetpack.svg",
        "m-2-jetpack.svg",
        "README.md"
      ],
      "repo": "jetpack",
      "path": "benchmarks/jmh-results-rigorous.json",
      "extractor": "jq -r '.[0].jdkVersion' \"$BLOB\" | cut -d. -f1",
      "note": "Read from the run that produced the scores, so the JDK on the plate is the JDK that was measured."
    },
    {
      "id": "jetpack.cores",
      "value": "10",
      "drawn_on": [
        "README.md"
      ],
      "repo": "jetpack",
      "path": "benchmarks/ENVIRONMENT.md",
      "extractor": "grep -oE '[0-9]+ logical' \"$BLOB\" | grep -oE '[0-9]+' | head -1"
    },
    {
      "id": "jetpack.confidence_level",
      "value": "99.9",
      "unit": "%",
      "drawn_on": [
        "README.md"
      ],
      "repo": "jetpack",
      "path": "benchmarks/ENVIRONMENT.md",
      "extractor": "grep -oE '99\\.9' \"$BLOB\" | head -1"
    },
    {
      "id": "jetpack.parallel_speedup_quick",
      "value": "6.89",
      "unit": "x",
      "drawn_on": [
        "README.md"
      ],
      "repo": "jetpack",
      "path": "benchmarks/jmh-results.json",
      "extractor": "jq -r '([.[]|select(.benchmark|endswith(\"parallelVirtualThreads\")).primaryMetric.score][0]/[.[]|select(.benchmark|endswith(\"singleThreadedJdk\")).primaryMetric.score][0]*100|round)/100' \"$BLOB\"",
      "note": "The number the page was suppressing. The parallel ratio moved 6.89x -> 6.38x between the quick and the rigorous run — an 8% spread, WIDER than the 4% spread on the SIMD ratio that the page already discloses. jetpack's own ENVIRONMENT.md warns about it. Publishing the smaller disagreement and hiding the larger one is the only place this page let the flattering number win, so the README now states it."
    },
    {
      "id": "jetpack.parallel_speedup_rigorous_2dp",
      "value": "6.38",
      "unit": "x",
      "drawn_on": [
        "README.md"
      ],
      "repo": "jetpack",
      "path": "benchmarks/jmh-results-rigorous.json",
      "extractor": "jq -r '([.[]|select(.benchmark|endswith(\"parallelVirtualThreads\")).primaryMetric.score][0]/[.[]|select(.benchmark|endswith(\"singleThreadedJdk\")).primaryMetric.score][0]*100|round)/100' \"$BLOB\"",
      "note": "The same ratio as jetpack.speedup, at two decimals, so the 6.89 -> 6.38 spread the README now discloses is stated in numbers that both derive."
    },
    {
      "id": "jetpack.adler_vector_speedup_quick",
      "value": "2.92",
      "unit": "x",
      "drawn_on": [
        "README.md"
      ],
      "repo": "jetpack",
      "path": "benchmarks/jmh-results.json",
      "extractor": "jq -r '([.[]|select(.benchmark|endswith(\"Adler32Benchmark.vector\")).primaryMetric.score][0]/[.[]|select(.benchmark|endswith(\"Adler32Benchmark.scalar\")).primaryMetric.score][0]*100|round)/100' \"$BLOB\""
    },
    {
      "id": "cadence.handlers",
      "value": "37",
      "drawn_on": [
        "README.md"
      ],
      "repo": "cadence",
      "path": "api/index.ts",
      "extractor": "awk '/^const ROUTES/,/^\\];/' \"$BLOB\" | grep -cE '^\\s*\\[\\['",
      "note": "The dispatch table itself, not the import list — at this pin both agree at 37 (an audit on 2026-08-10 confirmed 37 default handler imports, 37 ROUTES entries, 37 distinct handlers, with nothing imported-but-unrouted, nothing routed-but-unimported, no duplicate patterns and no route shadowed unreachable under api/index.ts's own match() ordering), but routes are what the function actually serves. Round 21: the parser plate is retired (the section merged into the refusal plate), so this is prose-only now."
    },
    {
      "id": "applied.macro_f1",
      "value": "0.979",
      "drawn_on": [
        "plate-4-applied.svg",
        "m-4-applied.svg",
        "README.md"
      ],
      "repo": "applied",
      "path": "backend/data/evaluation/baseline_rules_v3.json",
      "extractor": "jq -r '(.overall.macro_f1*1000|round)/1000' \"$BLOB\""
    },
    {
      "id": "applied.eval_set_size",
      "value": "96",
      "drawn_on": [
        "plate-4-applied.svg",
        "README.md"
      ],
      "repo": "applied",
      "path": "backend/data/evaluation/baseline_rules_v3.json",
      "extractor": "jq -r '.meta.sample_count' \"$BLOB\""
    },
    {
      "id": "applied.eval_classes",
      "value": "8",
      "drawn_on": [
        "README.md"
      ],
      "repo": "applied",
      "path": "backend/data/evaluation/baseline_rules_v3.json",
      "extractor": "jq -r '.label_distribution|keys|length' \"$BLOB\""
    },
    {
      "id": "applied.eval_mode_is_rules_only",
      "value": "rules",
      "drawn_on": [
        "plate-4-applied.svg",
        "README.md"
      ],
      "repo": "applied",
      "path": "backend/data/evaluation/baseline_rules_v3.json",
      "extractor": "jq -r '.meta.mode' \"$BLOB\"",
      "note": "This is the row that keeps the hero honest: the artifact itself says the score was produced with the rules layer alone, which is what the plate labels it."
    },
    {
      "id": "applied.regex_rules",
      "value": "201",
      "drawn_on": [
        "plate-4-applied.svg",
        "README.md"
      ],
      "repo": "applied",
      "path": "backend/jobtracker/classifier/rules.py",
      "extractor": "python3 -c \"import ast,sys;t=ast.parse(open(sys.argv[1]).read());print(sum(1 for n in ast.walk(t) if isinstance(n,ast.AnnAssign) and getattr(n.target,'id','')=='PATTERNS' for s in ast.walk(n.value) if isinstance(s,ast.Constant) and isinstance(s.value,str)))\" \"$BLOB\"",
      "note": "Parsed, not grepped. A second method — counting r-string lines inside the PATTERNS block — also gives 201."
    },
    {
      "id": "applied.confidence_gate",
      "value": "0.85",
      "drawn_on": [
        "plate-4-applied.svg",
        "m-4-applied.svg",
        "README.md"
      ],
      "repo": "applied",
      "path": "backend/jobtracker/cloud/pipeline.py",
      "extractor": "grep -E '^AUTO_FILE_GATE' \"$BLOB\" | grep -oE '[0-9]+\\.[0-9]+'",
      "note": "RE-POINTED 2026-08-10. This row used to extract REVIEW_QUEUE_CONFIDENCE_THRESHOLD from backend/jobtracker/api/classification.py:25 — the same 0.85, in a file the deployed function never evaluates. api/classification.py gates two desktop-only endpoints; main_cloud.py mounts only the applications/gmail/account cloud routers, and the cloud import graph never imports jobtracker.api at all (the only two 'jobtracker.api' strings under cloud/ are RST docstring prose). So the gate was certified against a constant that is dead on Vercel. AUTO_FILE_GATE (pipeline.py:410) is the one the hosted path actually compares against, at pipeline.py:746 inside _qualifies_for_hard_row; below-gate items go to collect_review_items (:827) and are persisted as category='needs_review' rows that a human resolves through apps/web/app/api/applications/review/[messageId]/classify. A right number read from the wrong file is the failure this whole file exists to prevent."
    },
    {
      "id": "applied.ci_floor",
      "value": "0.95",
      "drawn_on": [
        "plate-4-applied.svg",
        "README.md"
      ],
      "repo": "applied",
      "path": ".github/workflows/backend-ci.yml",
      "extractor": "grep -oE '\\-\\-min-macro-f1 [0-9.]+' \"$BLOB\" | grep -oE '[0-9]+\\.[0-9]+' | sort -u",
      "note": "The build really does fail below this: it is a CI flag, not a doc sentence."
    },
    {
      "id": "applied.onnx_fp32_mb",
      "value": "90.4",
      "unit": "MB",
      "drawn_on": [
        "plate-4-applied.svg",
        "README.md"
      ],
      "repo": "applied",
      "path": "ml/browser/artifacts/model.onnx",
      "bytes": true,
      "extractor": "awk -v b=\"$BYTES\" 'BEGIN{printf \"%.1f\", b/1e6}'",
      "note": "The byte length of the committed artifact, read from the contents API at that SHA. On main since 2026-08-03 (merge d349f02f, no PR) at byte-for-byte the size the branch pin carried — which is why the applied-web split could retire."
    },
    {
      "id": "applied.onnx_int8_mb",
      "value": "22.8",
      "unit": "MB",
      "drawn_on": [
        "plate-4-applied.svg",
        "README.md"
      ],
      "repo": "applied",
      "path": "ml/browser/artifacts/model_quantized.onnx",
      "bytes": true,
      "extractor": "awk -v b=\"$BYTES\" 'BEGIN{printf \"%.1f\", b/1e6}'"
    },
    {
      "id": "applied.hybrid_f1_prose",
      "value": "0.9583",
      "drawn_on": [
        "README.md"
      ],
      "repo": "applied",
      "path": "docs/ML_EXECUTION_TRACKER.md",
      "extractor": "sed -n '378p' \"$BLOB\" | grep -oE 'macro_f1=0\\.[0-9]+' | cut -d= -f2",
      "note": "The full three-layer cascade's score, which has no evaluation artifact because the run that produced it was overwritten. The page no longer says 'exactly one line of prose' — the number has since been restated in the repo's own README, and counting restatements was never the claim; the missing artifact is. This row proves the cited line still carries it."
    },
    {
      "id": "applied.hybrid_f1_line",
      "value": "378",
      "drawn_on": [
        "README.md"
      ],
      "repo": "applied",
      "path": "docs/ML_EXECUTION_TRACKER.md",
      "extractor": "grep -n 'macro_f1=0.9583' \"$BLOB\" | head -1 | cut -d: -f1",
      "note": "Pins the citation itself. If the tracker grows a line above 378, this fails and the README's reference is corrected rather than silently pointing at the wrong row."
    },
    {
      "id": "cadence.tenant_tables",
      "value": "7",
      "drawn_on": [
        "README.md"
      ],
      "repo": "cadence",
      "path": "lib/config/migrations/0002_enable_rls.sql",
      "extractor": "grep -cE '^ALTER TABLE .* FORCE ROW LEVEL SECURITY' \"$BLOB\"",
      "note": "Anchored to ^ALTER TABLE on purpose. A bare `grep -c 'FORCE ROW LEVEL SECURITY'` returns 8, because the file's own header comment explains what FORCE does — the same prose-counting mistake as the row above, in a file where it would have inflated the claim. user_profiles is deliberately excluded from RLS (0002_enable_rls.sql:21-22), so 'every tenant table' is seven, and the README now says seven.",
      "anchors": {
        "README.md": [
          "`FORCE`d on all **seven** tenant tables"
        ]
      }
    },
    {
      "id": "jetpack.adler_known_answer",
      "value": "11E60398",
      "drawn_on": [
        "plate-2-jetpack.svg"
      ],
      "repo": "jetpack",
      "path": "src/test/java/com/ayush/jetpack/Adler32Test.java",
      "extractor": "grep -oE '0x[0-9A-F]{8}L' \"$BLOB\" | sort -u | head -1 | sed 's/^0x//; s/L$//'",
      "note": "The canonical Adler-32 vector for the input \"Wikipedia\", asserted for both the scalar and the vectorised path (Adler32Test.java:36-37). It is now drawn as one string per row rather than eight <text> elements, so it appears whole in the plate and needs no per-glyph exemptions. The stronger claim on the plate — bit-identical to java.util.zip — is the separate matchesJdkAcrossSizes test, which compares both paths across 26 input sizes."
    },
    {
      "id": "cadence.idor_services",
      "value": "6",
      "drawn_on": [
        "plate-5-refusal.svg",
        "README.md"
      ],
      "repo": "cadence",
      "paths": [
        "lib/services/AttachmentService.ts",
        "lib/services/CalendarService.ts",
        "lib/services/EventService.ts",
        "lib/services/TagService.ts",
        "lib/services/TaskListService.ts",
        "lib/services/TaskService.ts"
      ],
      "extractor": "grep -lE 'AND \"userId\" = [$]' $BLOBS | grep -c .",
      "note": "Counted from the guard in the QUERY, not from a comment about it. The previous row here counted the word IDOR in docstrings and returned 7; four different methods return 7, 8, 9 and 15, so the count of vulnerabilities is an audit finding and belongs in prose. How many services now scope by owner is a property of the code."
    },
    {
      "id": "glyph.deployed_wasm_sha256",
      "value": "e681d2f76d41305aa3b8c250799f898bd1139497f60580ed59000d49cf5d6360",
      "drawn_on": [
        "README.md"
      ],
      "repo": "glyph",
      "path": "web/public/wasm/fast_mnist.wasm",
      "extractor": "shasum -a 256 \"$BLOB\" | cut -d' ' -f1",
      "note": "The page's strongest single piece of evidence. The binary getglyph.vercel.app serves is byte-identical to this blob on main, so 'the deployment is what the repository builds' stops being testimony: curl the URL, hash it, compare. It used to point at an unmerged branch, because that was where the SIMD build lived; it merged, so this reads main. THE PAIR IS THE CLAIM AND NEITHER HALF PROVES IT: this row hashes the REPO blob and glyph.deployed_wasm_bytes reads the DEPLOYMENT's content-length, so the two are checked against different sources and can drift apart while both stay green. That is exactly what happened on 2026-08-08 — the deployment rebuilt, bytes went red, and this hash stayed green against a stale pin. Move them together, always."
    },
    {
      "id": "applied.eval_misclassified",
      "value": "2",
      "drawn_on": [
        "README.md",
        "plate-4-applied.svg"
      ],
      "repo": "applied",
      "path": "backend/data/evaluation/baseline_rules_v3.json",
      "extractor": "jq -r '.overall.misclassified' \"$BLOB\"",
      "note": "0.979 with no uncertainty beside it was the page's one remaining asymmetry: jetpack's numbers all carry an interval and Applied's did not. The honest statement of a 96-row result is the error count. Round 21: the rebuilt plate prints it too — '2 MISTAKES' beside the hero."
    },
    {
      "id": "applied.eval_per_class",
      "value": "12",
      "drawn_on": [
        "README.md"
      ],
      "repo": "applied",
      "path": "backend/data/evaluation/baseline_rules_v3.json",
      "extractor": "jq -r '.label_distribution|to_entries|map(.value)|unique|.[0]' \"$BLOB\"",
      "note": "The set is balanced 12 per class, which is why macro-F1 and weighted-F1 are identical to the last digit. Unstated until now, and it is the reason the two numbers agree."
    },
    {
      "id": "automl.license",
      "value": "GPL-3.0",
      "drawn_on": [
        "plate-6b-automl.svg",
        "README.md"
      ],
      "repo": "automl",
      "path": "LICENSE",
      "extractor": "grep -qE '^ *GNU GENERAL PUBLIC LICENSE' \"$BLOB\" && grep -qE '^ *Version 3,' \"$BLOB\" && echo GPL-3.0",
      "note": "The page asserted PolyForm Noncommercial because a relicence PR had been opened. A proposed licence is not a licence: PR #5 is still open, so the pinned commit is GPL-3.0 and that is what the page now says. Re-pin and change this row when it merges."
    },
    {
      "id": "automl.tools",
      "value": "44",
      "drawn_on": [
        "plate-6b-automl.svg",
        "m-6b-automl.svg",
        "README.md"
      ],
      "repo": "automl",
      "paths": [
        "backend/src/services/llm/tools/dataTools.ts",
        "backend/src/services/llm/tools/cellTools.ts",
        "backend/src/services/llm/tools/packageTools.ts",
        "backend/src/services/llm/tools/featureTools.ts",
        "backend/src/services/llm/tools/preprocessingTools.ts",
        "backend/src/services/llm/tools/trainingTools.ts",
        "backend/src/services/llm/tools/uiTools.ts"
      ],
      "extractor": "cat $BLOBS | grep -cE \"^\\s+name: '\"",
      "note": "Counted two ways and they agree: the seven files sum 4+8+3+6+14+6+3, and a single grep across all seven returns 44."
    },
    {
      "id": "automl.tools_general",
      "value": "15",
      "drawn_on": [
        "plate-6b-automl.svg",
        "README.md"
      ],
      "repo": "automl",
      "paths": [
        "backend/src/services/llm/tools/dataTools.ts",
        "backend/src/services/llm/tools/cellTools.ts",
        "backend/src/services/llm/tools/packageTools.ts"
      ],
      "extractor": "cat $BLOBS | grep -cE \"^\\s+name: '\"",
      "note": "LLM_TOOL_DEFINITIONS in tools/index.ts is data + cell + package only: 4 data + 8 cell + 3 package. CORRECTED 2026-08-10 — this row previously read 'the general set the model carries everywhere is 15 of them', and the README said the same. It is not carried everywhere. No phase receives all 15: training admits only FEATURE_DISCOVERY_TOOLS union NOTEBOOK_EXECUTION_TOOLS (tools/index.ts:117-119), which excludes all three package tools, and re-adds install_package at the generate_code/write_code stages only; onboarding takes four (index.ts:130-137); preprocessing takes none, building its own list instead. The array length is still exactly what this extractor gates and is still worth drawing — what changed is the sentence around it. Same failure mode as [automl.tool_sets]: the count was right and the verb was wrong."
    },
    {
      "id": "automl.tool_sets",
      "value": "7",
      "drawn_on": [
        "plate-6b-automl.svg",
        "README.md"
      ],
      "repo": "automl",
      "path": "backend/src/services/llm/tools/index.ts",
      "extractor": "grep -E '^export const LLM_[A-Z_]+_TOOLS' \"$BLOB\" | grep -vc LLM_ALL_TOOLS",
      "note": "Seven exported phase sets: onboarding, preprocessing, feature proposal, feature continue, feature engineering, feature lifecycle, training lifecycle. LLM_ALL_TOOLS is excluded because it is the union, not a phase. WARNING — this extractor counts EXPORTS, not ROUTES, and the two are not the same number. A source audit at this pin found LLM_PREPROCESSING_TOOLS (index.ts:124) and LLM_FEATURE_ENGINEERING_TOOLS (index.ts:61) are exported and never imported: their only other occurrence in the tree is the barrel re-export at toolRegistry.ts:18,23, there is no phase->set lookup table (BY_PHASE|PHASE_TOOLS|TOOL_SETS|toolsForPhase all return nothing repo-wide), and no `export *` or namespace import can be hiding a consumer. The preprocessing phase does receive its tools, assembled ad hoc from PREPROCESSING_ORCHESTRATION_TOOLS + CELL_TOOL_DEFINITIONS at phases/preprocessing/stageConfig.ts:21. So five of the seven names are live routes. The README said 'the other 29 arrive with the phase that needs them, through seven exported sets' — a true count describing a false mechanism, the same shape as the count-noun trap that produced AutoML's 44-vs-12 error. The prose now names the two dead exports; this row keeps gating the export count, which is all a grep of one file can honestly gate.",
      "anchors": {
        "README.md": [
          "Of the seven declared per-phase sets",
          "**Seven** named per-phase sets are declared"
        ]
      }
    },
    {
      "id": "automl.tables",
      "value": "29",
      "drawn_on": [
        "plate-6b-automl.svg",
        "README.md"
      ],
      "repo": "automl",
      "paths": [
        "backend/migrations/001_init.sql",
        "backend/migrations/002_auth.sql",
        "backend/migrations/004_notebooks.sql",
        "backend/migrations/007_workflows.sql",
        "backend/migrations/009_savepoints.sql",
        "backend/migrations/010_experiments.sql",
        "backend/migrations/011_nl_placeholder_suggestions.sql",
        "backend/migrations/014_models.sql",
        "backend/migrations/016_plan_chats.sql",
        "backend/migrations/017_deployments.sql",
        "backend/migrations/019_email_verification_tokens.sql"
      ],
      "extractor": "cat $BLOBS | grep -ciE '^create table'",
      "note": "Two methods agree: 29 CREATE TABLE statements, and 29 distinct table names after lowercasing and deduplicating. Eleven of the 24 migrations create tables (7+3+3+6+1+1+1+1+1+4+1); the other thirteen alter, backfill or index, and listing them would only pad the fetch."
    },
    {
      "id": "automl.sandbox_tmpfs",
      "value": "5",
      "drawn_on": [
        "plate-6b-automl.svg",
        "README.md"
      ],
      "repo": "automl",
      "path": "backend/src/services/container/dockerBuilder.ts",
      "extractor": "grep -c -- '--tmpfs' \"$BLOB\"",
      "note": "The container is --read-only on an --internal network, so these five tmpfs mounts are the entire writable surface the generated Python gets. Drawing the count is drawing the size of the blast radius.",
      "anchors": {
        "README.md": [
          "Five `--tmpfs` mounts"
        ]
      }
    },
    {
      "id": "glyph.dot256_speedup",
      "value": "3.5",
      "unit": "x",
      "drawn_on": [
        "plate-1-glyph.svg",
        "m-1-glyph.svg",
        "README.md"
      ],
      "repo": "glyph",
      "paths": [
        "docs/benchmarks/runs/bench-20260802-dot20x-baseline.json",
        "docs/benchmarks/runs/bench-20260802-dot20x-openmp-native.json"
      ],
      "extractor": "jq -s -r '[.[]|[.benchmarks[]|select(.name==\"benchDot/256_median\")][0].real_time]|@tsv' $BLOBS | awk '{printf \"%.1f\", $1/$2}'",
      "note": "benchDot/256, openmp+native against the plain course baseline, derived as a ratio of two committed medians rather than stored. Re-sourced 2026-08-10 from docs/benchmarks/bench_summary.csv to the 20-repetition dot20x pair, for the reason recorded on glyph.axpy128_slowdown: the CSV is the December record on a different machine, and glyph's ENVIRONMENT.md names the 2026-08-02 M1 Pro runs as the reference. This is also the pair glyph's own README calls the figure to cite -- tightest coefficient of variation on record for this kernel, 0.2% and 0.3%. Three artifact-backed readings span 3.504x (CSV), 3.536x (dot20x, 20 reps) and 3.570x (aggregated, 10 reps); the first two round to 3.5 and the third to 3.6, so WHICH artifact is canonical decides the drawn digit. That is the whole argument for retiring the CSV rather than keeping both."
    },
    {
      "id": "glyph.axpy128_slowdown",
      "value": "10.7",
      "unit": "x slower",
      "drawn_on": [
        "plate-1-glyph.svg",
        "README.md"
      ],
      "repo": "glyph",
      "paths": [
        "docs/benchmarks/runs/bench-20260802-aggregated-baseline.json",
        "docs/benchmarks/runs/bench-20260802-aggregated-openmp-native.json"
      ],
      "extractor": "jq -s -r '[.[]|[.benchmarks[]|select(.name==\"benchAxpy/128_median\")][0].real_time]|@tsv' $BLOBS | awk '{printf \"%.1f\", $2/$1}'",
      "note": "The answer that went against the author, printed on purpose: the same openmp+native flags that win benchDot/256 LOSE benchAxpy/128. Below a size floor, threading costs more than it pays, and the page says so. This read 6.9x until 2026-08-10, sourced to docs/benchmarks/bench_summary.csv. That figure was arithmetically correct and pointed at the wrong machine: bench_summary.csv is the December record, taken on Shrees-MacBook.local, and glyph's own docs/benchmarks/ENVIRONMENT.md retires it in one line -- 'The 2026-08-02 runs on the M1 Pro are the reference. The December runs are history.' For an OpenMP scaling ratio a fanless 4-performance-core machine is a different ceiling, not a noisier reading of the same one, which is why the two baselines agree within 8% while the two parallel numbers differ by 69%. Both glyph ratios now derive from the 2026-08-02 reference machine. They come from two runs on it because only one of them measures axpy: the dot20x pair carries no benchAxpy rows at all."
    },
    {
      "id": "visualassist.swift_lines",
      "value": "7177",
      "drawn_on": [
        "plate-8-visualassist.svg",
        "README.md"
      ],
      "repo": "visualassist",
      "paths": [
        "Assets/generate_banner.swift",
        "Assets/generate_icon.swift",
        "Assets/generate_social_preview.swift",
        "VisualAssist/App/AppState.swift",
        "VisualAssist/App/VisualAssistApp.swift",
        "VisualAssist/Models/DetectedObject.swift",
        "VisualAssist/Models/DetectedObstacle.swift",
        "VisualAssist/Models/RecognizedText.swift",
        "VisualAssist/Models/UserSettings.swift",
        "VisualAssist/Services/CameraService.swift",
        "VisualAssist/Services/HapticService.swift",
        "VisualAssist/Services/LiDARService.swift",
        "VisualAssist/Services/ObjectDetectionService.swift",
        "VisualAssist/Services/SpeechService.swift",
        "VisualAssist/Services/TextRecognitionService.swift",
        "VisualAssist/Services/VoiceCommandService.swift",
        "VisualAssist/Utilities/AccessibilityHelper.swift",
        "VisualAssist/Utilities/DepthProcessor.swift",
        "VisualAssist/Utilities/SpatialAudioManager.swift",
        "VisualAssist/Views/Components/AccessibleButton.swift",
        "VisualAssist/Views/Components/CameraPreview.swift",
        "VisualAssist/Views/Components/ModeCard.swift",
        "VisualAssist/Views/Components/StatusOverlay.swift",
        "VisualAssist/Views/ContentView.swift",
        "VisualAssist/Views/DeviceLevelIndicator.swift",
        "VisualAssist/Views/HomeView.swift",
        "VisualAssist/Views/NavigationModeView.swift",
        "VisualAssist/Views/ObjectAwarenessModeView.swift",
        "VisualAssist/Views/SettingsView.swift",
        "VisualAssist/Views/TextReadingModeView.swift",
        "VisualAssistTests/AccessibilityHelperTests.swift",
        "VisualAssistTests/CommonObjectLabelTests.swift",
        "VisualAssistTests/DepthProcessorTests.swift",
        "VisualAssistTests/DetectedObstacleTests.swift",
        "VisualAssistTests/DetectionSummaryTests.swift",
        "VisualAssistTests/ObjectCategoryTests.swift",
        "VisualAssistTests/ObjectPositionTests.swift",
        "VisualAssistTests/RecognizedTextTests.swift"
      ],
      "extractor": "cat $BLOBS | wc -l | tr -d ' '",
      "note": "wc -l over every .swift blob at the pin. The paths listed ARE the evidence for the companion file count: the recursive tree API at this SHA returns exactly these 38 and no others (verify: curl -s https://api.github.com/repos/yadava5/VisualAssist/git/trees/b0e6203369a829e2174ea263be53be0287675af9?recursive=1 | jq '[.tree[]|select(.path|endswith(\".swift\"))]|length')."
    },
    {
      "id": "visualassist.swift_files",
      "value": "38",
      "drawn_on": [
        "plate-8-visualassist.svg",
        "README.md"
      ],
      "repo": "visualassist",
      "paths": [
        "Assets/generate_banner.swift",
        "Assets/generate_icon.swift",
        "Assets/generate_social_preview.swift",
        "VisualAssist/App/AppState.swift",
        "VisualAssist/App/VisualAssistApp.swift",
        "VisualAssist/Models/DetectedObject.swift",
        "VisualAssist/Models/DetectedObstacle.swift",
        "VisualAssist/Models/RecognizedText.swift",
        "VisualAssist/Models/UserSettings.swift",
        "VisualAssist/Services/CameraService.swift",
        "VisualAssist/Services/HapticService.swift",
        "VisualAssist/Services/LiDARService.swift",
        "VisualAssist/Services/ObjectDetectionService.swift",
        "VisualAssist/Services/SpeechService.swift",
        "VisualAssist/Services/TextRecognitionService.swift",
        "VisualAssist/Services/VoiceCommandService.swift",
        "VisualAssist/Utilities/AccessibilityHelper.swift",
        "VisualAssist/Utilities/DepthProcessor.swift",
        "VisualAssist/Utilities/SpatialAudioManager.swift",
        "VisualAssist/Views/Components/AccessibleButton.swift",
        "VisualAssist/Views/Components/CameraPreview.swift",
        "VisualAssist/Views/Components/ModeCard.swift",
        "VisualAssist/Views/Components/StatusOverlay.swift",
        "VisualAssist/Views/ContentView.swift",
        "VisualAssist/Views/DeviceLevelIndicator.swift",
        "VisualAssist/Views/HomeView.swift",
        "VisualAssist/Views/NavigationModeView.swift",
        "VisualAssist/Views/ObjectAwarenessModeView.swift",
        "VisualAssist/Views/SettingsView.swift",
        "VisualAssist/Views/TextReadingModeView.swift",
        "VisualAssistTests/AccessibilityHelperTests.swift",
        "VisualAssistTests/CommonObjectLabelTests.swift",
        "VisualAssistTests/DepthProcessorTests.swift",
        "VisualAssistTests/DetectedObstacleTests.swift",
        "VisualAssistTests/DetectionSummaryTests.swift",
        "VisualAssistTests/ObjectCategoryTests.swift",
        "VisualAssistTests/ObjectPositionTests.swift",
        "VisualAssistTests/RecognizedTextTests.swift"
      ],
      "extractor": "printf '%s\\n' $BLOBS | grep -c .",
      "note": "Counts the fetched blobs, so on its own it proves these 38 EXIST at the pin; that no 39th exists is the tree-API check in the row above. Honest about what each half proves."
    },
    {
      "id": "visualassist.critical_m",
      "value": "0.5",
      "unit": "m",
      "drawn_on": [
        "README.md",
        "plate-8-visualassist.svg",
        "m-8-visualassist.svg"
      ],
      "repo": "visualassist",
      "path": "VisualAssist/Services/LiDARService.swift",
      "extractor": "sed -n 's/.*criticalDistance: Float = \\([0-9.]*\\).*/\\1/p' \"$BLOB\"",
      "note": "The three thresholds below are the only numbers section VII draws that describe a DECISION rather than an amount of effort. Lines of code and file counts measure how much was typed; these measure what the app does to a person walking. They are private `let` constants on LiDARService — and round 25 found the sharper fact this note used to soften. It said 'the Settings sliders do not reach them, so a user cannot tune the distance', which reads as a design choice. It is not: SettingsView.swift draws a real Slider bound to @AppStorage('alertDistance'), with an accessibilityValue read aloud, and LiDARService never references UserSettings at all. The control exists, is reachable by a low-vision user, announces its value, and changes nothing. That is a defect in an accessibility app, not a decision, and the page now says so."
    },
    {
      "id": "visualassist.warning_m",
      "value": "1.0",
      "unit": "m",
      "drawn_on": [
        "README.md",
        "plate-8-visualassist.svg"
      ],
      "repo": "visualassist",
      "path": "VisualAssist/Services/LiDARService.swift",
      "extractor": "sed -n 's/.*warningDistance: Float = \\([0-9.]*\\).*/\\1/p' \"$BLOB\""
    },
    {
      "id": "visualassist.caution_m",
      "value": "2.0",
      "unit": "m",
      "drawn_on": [
        "README.md",
        "plate-8-visualassist.svg",
        "m-8-visualassist.svg"
      ],
      "repo": "visualassist",
      "path": "VisualAssist/Services/LiDARService.swift",
      "extractor": "sed -n 's/.*cautionDistance: Float = \\([0-9.]*\\).*/\\1/p' \"$BLOB\"",
      "note": "The widest ring is the one that says the most about the design: inside 2.0 m the obstacle is drawn on screen and deliberately NOT announced. An aid that narrates every wall is an aid people switch off, so silence here is a feature with a threshold attached, not an omission."
    },
    {
      "id": "visualassist.workflows",
      "value": "5",
      "drawn_on": [
        "plate-8-visualassist.svg",
        "README.md"
      ],
      "repo": "visualassist",
      "paths": [
        ".github/workflows/ci.yml",
        ".github/workflows/codeql.yml",
        ".github/workflows/gitleaks.yml",
        ".github/workflows/release.yml",
        ".github/workflows/scorecard.yml"
      ],
      "extractor": "grep -lE '^(on|name):' $BLOBS | grep -c .",
      "note": "Five real workflow files at the pin — build, CodeQL, gitleaks, release, scorecard — each verified to be a workflow (a top-level on:/name: key), not just a file that exists."
    },
    {
      "id": "visualassist.spatial_audio_files",
      "value": "3",
      "unit": "files",
      "drawn_on": [
        "README.md"
      ],
      "anchors": {
        "README.md": [
          "its name occurs in exactly three files and nowhere else"
        ]
      },
      "repo": "visualassist",
      "tree": true,
      "extractor": "n=$(grep -rl SpatialAudioManager \"$TREE\" | grep -c .); u=$(grep -rE 'SpatialAudioManager\\(|: *SpatialAudioManager' \"$TREE\" | grep -c .); printf '%s' \"$n\"; [ \"$u\" = 0 ] || printf '+%s-uses' \"$u\"",
      "note": "The first row on this page about a WHOLE repository rather than a named file, and it has to be: 'nothing constructs this class' is falsified by any one of 38 files, so checking three named blobs proves nothing about the fourth. Fetches the pinned tarball and greps the tree. Deliberately NOT an expected-zero grep — a `| wc -l` that must print 0 also prints 0 when the pattern is typo'd or the escaping is wrong, which is the check-that-cannot-fail defect this repo has shipped four times, wearing a new uniform. Measured: breaking the class name takes the count to 0, not to 3, so the row fails when its own instrument fails. The construction count rides in the SAME value rather than a second row — if a call site ever appears the output becomes '3+1-uses' and this goes red, instead of staying quietly true about the file count while the sentence built on it turns false."
    }
  ],
  "$unpinnable_note": [
    "This list held exactly one row — AutoML's '2 approval gates' — filed under",
    "the reason 'the repo is private'. That diagnosis was wrong, and the way it",
    "was wrong matters more than the number.",
    "",
    "The count was not merely unverifiable. It was FALSE: the reproducible count",
    "of human-gate stages is four (preprocessing await_approval, feature",
    "engineering await_review, training propose_model and await_review). By",
    "filing it as 'private, therefore unpinnable' this file certified a wrong",
    "number as an acceptable one, and printed a friendly note where it should",
    "have failed. A category that excuses a claim from being checked will,",
    "sooner or later, be where a false claim goes to live.",
    "",
    "So there is no such category any more. Plate VI no longer draws a count at",
    "all; it draws the mechanism and says on its face that this is the one",
    "section a reader cannot check. Anything drawn must derive or be exempt."
  ],
  "unpinnable": [],
  "external": [
    {
      "id": "cadence.function_cap",
      "value": "12",
      "drawn_on": [
        "README.md"
      ],
      "source": "https://vercel.com/docs/functions/runtimes#functions-created-per-deployment",
      "why": "Vercel's Hobby-tier cap of 12 Vercel Functions per deployment is a platform fact (the docs moved: /docs/limits now says 'framework-dependent', and the 12 lives at the runtimes page cited above), not a measurement of this repository. The previous extractor grepped `caps deployments at 12` out of api/index.ts — the author's own comment — so the page was citing itself and calling it derivation. What the repo genuinely proves is the consequence: one function serving 37 routes, which cadence.handlers derives. (That read 36 until the 2026-08-10 re-pin moved cadence.handlers to 37 — a note inside the provenance file itself going stale, one row away from the gate that would have caught it had it been a drawn number.)"
    }
  ],
  "exempt": {
    "$comment": [
      "Numbers a plate draws that are not measurements of a repository — names,",
      "versions, viewBox geometry, and the glyphs of a hex constant. Each needs a",
      "reason. Prefer a file-scoped key ('plate-2-jetpack.svg:6') over a bare one:",
      "a global exemption for '6' would wave through an unsourced 6 anywhere in",
      "the document. claims.mjs also fails on an exemption no plate uses, so this",
      "list cannot quietly accumulate excuses for numbers that are already gone."
    ],
    "plate-1-glyph.svg:512": "AVX-512 — an instruction-set name. (AVX2, e5, int8 and 1pm need no entry: there is no word boundary before those digits, so they are never read as numbers.)",
    "plate-1-glyph.svg:4": "'4 BUILDS · 1 PATH COMPILED', counting the four build targets drawn directly above it",
    "plate-1-glyph.svg:1": "'4 BUILDS · 1 PATH COMPILED' and '3 BY HAND, 1 AUTO' — the same four targets, counted",
    "plate-1-glyph.svg:3": "'3 BY HAND, 1 AUTO'",
    "plate-2-jetpack.svg:32": "the name of the Adler-32 algorithm",
    "plate-2-jetpack.svg:25": "JDK 25 — a toolchain version, not a measurement",
    "plate-6b-automl.svg:3.0": "the version field of the licence identifier GPL-3.0, which automl.license derives whole from the pinned LICENSE file",
    "README.md:3.0": "the version field of the licence identifier GPL-3.0 (see automl.license)",
    "m-2-jetpack.svg:25": "JDK 25 — a toolchain version, not a measurement",
    "plate-2-jetpack.svg:3": "'3 JMH FORKS' — the fork count of the run, recorded in the same JSON the scores come from",
    "README.md:512": "AVX-512 — an instruction-set name",
    "README.md:32": "the name of the Adler-32 algorithm",
    "README.md:1": "the placeholder in the SQL literal `WHERE id = $1`",
    "README.md:4": "'four ship a system card' — enumerated on the page (Glyph, jetpack, Cadence, Applied); the fifth and sixth are named as a booklet and a no-link app",
    "README.md:219": "the first line of the early-stopping block cited as apps/train_model.cpp:219-243. A line RANGE has no single extractor; the citation is verified by reading it, and glyph.msimd128_line pins the one line citation that a machine can check.",
    "README.md:243": "the last line of that same cited range",
    "README.md:03.23": "part of the contact address aesh.03.23@gmail.com",
    "m-0-thesis.svg:6": "'6 systems' — a count of this page's own contents, drawn as the mobile thesis hero. This sentence used to cite matching exemptions on README.md and plate-7-colophon.svg as though they still stood; both are gone, and it kept naming them until 2026-08-10. A justification is prose, the gate reads numbers, and so a note explaining an exemption outlived two of the three things it described — the same failure the colophon now names on the page itself.",
    "README.md:256": "the '256' in `shasum -a 256` — the name of the hash algorithm in a command the reader is invited to run — and the benchDot/256 case name",
    "plate-0-thesis.svg:03.23": "part of the contact address aesh.03.23@gmail.com",
    "README.md:2026": "the graduation date, May 2026 — a fact about the author, not a measurement of a repository",
    "plate-0b-work.svg:2025": "'Jun 2025 – May 2026' — the dates of the role",
    "plate-0b-work.svg:2026": "'Jun 2025 – May 2026', and 'DATAFEST 2026'",
    "plate-0b-work.svg:5": "'5 years' — the span of query logs ingested",
    "plate-0b-work.svg:0": "'FROM 0' — the compliance baseline the 96.72% is measured against",
    "plate-0b-work.svg:3": "'3-PERSON' — the DataFest team size",
    "README.md:2025": "'June 2025 to May 2026' — the dates of the role",
    "README.md:0": "'from 0% to 96.72%' — the compliance baseline",
    "README.md:3": "'a three-layer cascade' — the count of the tiers named in the same sentence; 'three committed runs' in section III, which BENCHMARKS.md documents; and section V's 'three carry it in the query and the other three check ownership first', the guard split the plate draws as filled and hollow marks",
    "plate-1-glyph.svg:256": "benchDot/256 — the benchmark case's size parameter, part of its name in bench_summary.csv (glyph.dot256_speedup derives the ratio itself)",
    "plate-1-glyph.svg:128": "benchAxpy/128 — likewise the case's size parameter (glyph.axpy128_slowdown derives the ratio)",
    "README.md:128": "benchAxpy/128 — the benchmark case name (see plate-1-glyph.svg:128)"
  },
  "$attested_note": [
    "Facts about the AUTHOR rather than measurements of a repository.",
    "",
    "Everything in `claims` is re-derived in CI from a pinned commit, and that is",
    "the page's whole argument. A year of internship work is not in any repository",
    "a reader can clone: the Oracle logs, the Tableau inventory and the compliance",
    "dashboard belong to Miami University. Nor is a competition result.",
    "",
    "Those facts are worth putting on the page, and pretending they carry the same",
    "warrant as `422 MB/s` would be exactly the move this document exists to",
    "refuse. So they live here, they are labelled ATTESTED on the plate that draws",
    "them, and claims.mjs prints them separately from the derived set. The reader",
    "is told which numbers they can check and which they are taking on trust."
  ],
  "attested": [
    {
      "id": "work.query_logs",
      "value": "1.6",
      "unit": "M Oracle Analytics query logs",
      "drawn_on": [
        "plate-0b-work.svg",
        "m-0b-work.svg",
        "README.md"
      ],
      "source": "Ayush Yadav CV, July 2026",
      "why": "the work belongs to Miami University / a competition dataset; no public repository can be pinned"
    },
    {
      "id": "work.field_usage_rows",
      "value": "57.8",
      "unit": "M-row field-usage table",
      "drawn_on": [
        "plate-0b-work.svg",
        "m-0b-work.svg",
        "README.md"
      ],
      "source": "Ayush Yadav CV, July 2026",
      "why": "the work belongs to Miami University / a competition dataset; no public repository can be pinned"
    },
    {
      "id": "work.inventory_rows",
      "value": "10453",
      "unit": "row master asset inventory",
      "drawn_on": [
        "plate-0b-work.svg",
        "README.md"
      ],
      "source": "Ayush Yadav CV, July 2026",
      "why": "the work belongs to Miami University / a competition dataset; no public repository can be pinned"
    },
    {
      "id": "work.compliance_after",
      "value": "96.72",
      "unit": "% code compliance, from 0",
      "drawn_on": [
        "plate-0b-work.svg",
        "README.md"
      ],
      "source": "Ayush Yadav CV, July 2026",
      "why": "the work belongs to Miami University / a competition dataset; no public repository can be pinned"
    },
    {
      "id": "work.projects",
      "value": "61",
      "unit": "project portfolio",
      "drawn_on": [
        "plate-0b-work.svg",
        "README.md"
      ],
      "source": "Ayush Yadav CV, July 2026",
      "why": "the work belongs to Miami University / a competition dataset; no public repository can be pinned"
    },
    {
      "id": "datafest.patients",
      "value": "349",
      "unit": "K patients modelled",
      "drawn_on": [
        "plate-0b-work.svg",
        "README.md"
      ],
      "source": "Ayush Yadav CV, July 2026",
      "why": "the work belongs to Miami University / a competition dataset; no public repository can be pinned"
    },
    {
      "id": "datafest.auc",
      "value": "0.90",
      "unit": "holdout AUC",
      "drawn_on": [
        "plate-0b-work.svg",
        "README.md"
      ],
      "source": "Ayush Yadav CV, July 2026",
      "why": "the work belongs to Miami University / a competition dataset; no public repository can be pinned"
    },
    {
      "id": "datafest.encounters",
      "value": "7.7",
      "unit": "M encounters, 1.4 GB",
      "drawn_on": [
        "plate-0b-work.svg",
        "README.md"
      ],
      "source": "Ayush Yadav CV, July 2026",
      "why": "the work belongs to Miami University / a competition dataset; no public repository can be pinned"
    },
    {
      "id": "datafest.linkage",
      "value": "99.6",
      "unit": "% social-determinant linkage preserved",
      "drawn_on": [
        "plate-0b-work.svg",
        "README.md"
      ],
      "source": "Ayush Yadav CV, July 2026",
      "why": "the work belongs to Miami University / a competition dataset; no public repository can be pinned"
    },
    {
      "id": "datafest.naive_linkage",
      "value": "32",
      "unit": "% under a naive join",
      "drawn_on": [
        "plate-0b-work.svg",
        "README.md"
      ],
      "source": "Ayush Yadav CV, July 2026",
      "why": "the work belongs to Miami University / a competition dataset; no public repository can be pinned"
    },
    {
      "id": "datafest.gigabytes",
      "value": "1.4",
      "unit": "GB of encounter data",
      "drawn_on": [
        "README.md"
      ],
      "source": "Ayush Yadav CV, July 2026",
      "why": "the work belongs to Miami University / a competition dataset; no public repository can be pinned"
    },
    {
      "id": "datafest.window_days",
      "value": "90",
      "unit": "day care-utilisation window",
      "drawn_on": [
        "plate-0b-work.svg",
        "README.md"
      ],
      "source": "Ayush Yadav CV, July 2026",
      "why": "the work belongs to Miami University / a competition dataset; no public repository can be pinned"
    },
    {
      "id": "awards.mucat_grant",
      "value": "2500",
      "unit": "dollar MUCAT prototyping grant",
      "drawn_on": [
        "README.md"
      ],
      "source": "Ayush Yadav CV, July 2026",
      "why": "the work belongs to Miami University / a competition dataset; no public repository can be pinned"
    },
    {
      "id": "datafest.team_size",
      "value": "3",
      "drawn_on": [
        "README.md"
      ],
      "anchors": {
        "README.md": [
          "I led a three-person team"
        ]
      },
      "source": "Ayush Yadav CV, July 2026",
      "why": "Team size at DataFest 2026. Attested like the rest of §I, and anchored to its own sentence so the number cannot be changed under cover of another row's 3."
    }
  ]
}
